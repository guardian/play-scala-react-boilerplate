# play-scala-react-boilerplate

# Getting Started
Install the npm modules: `npm install`

# Running the app
1. `./scripts/start.sh` (This will bundle the JS and run the app)
2. Navigate to [localhost:9000](localhost:9000)
